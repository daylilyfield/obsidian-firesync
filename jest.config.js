module.exports = {
  preset: 'ts-jest',
  resetMocks: true,
  restoreMocks: true,
  // collectCoverage: true,
  // coverageDirectory: 'coverage',
  // coverageProvider: 'v8',
  moduleDirectories: ['node_modules', 'src'],
}
