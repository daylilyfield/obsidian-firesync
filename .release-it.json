{
  "git": {
    "commitMessage": "release: v${version}",
    "tag": true,
    "tagName": "${version}",
    "tagAnnotation": "${version}",
    "push": true,
    "pushArgs": ["--follow-tags"]
  },
  "npm": {
    "publish": false
  }
}
